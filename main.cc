#include <iostream>
#include <string>

#include "Encrypt.h"
#include "Decrypt.h"

using namespace std;

int main(){
	string line;
	vector<string> 
	cout<<"Text to encrypt: "<<endl;
	getline(cin, line);
	cout<<line;
}